<UserControl x:Class="DockerBuildBoxSystem.App.UserControls.ShutdownOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="Auto" Width="Auto">
    <Grid Background="#CC000000">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <materialDesign:PackIcon x:Name="SpinnerIcon" Kind="Refresh" Width="48" Height="48" Foreground="White" HorizontalAlignment="Center">
                <materialDesign:PackIcon.RenderTransform>
                    <RotateTransform Angle="0" CenterX="24" CenterY="24"/>
                </materialDesign:PackIcon.RenderTransform>
                <materialDesign:PackIcon.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
                </materialDesign:PackIcon.Effect>
            </materialDesign:PackIcon>
            <TextBlock Text="Shutting down..." Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,12,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="Please wait" Foreground="White" Opacity="0.9" FontSize="14" HorizontalAlignment="Center" Margin="0,6,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="1" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>
            <StackPanel.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation Storyboard.TargetName="SpinnerIcon"
                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                             From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </StackPanel.Triggers>
        </StackPanel>
    </Grid>
</UserControl>